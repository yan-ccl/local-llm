# 黑名单：确定无疑的错词对，格式「错 对」（空格或制表符分隔）。
# rules 层以 0.99 置信度直接改正，优先级最高。
# 收录原则：只放几乎不依赖上下文、整词匹配后改了基本不会错的词，宁缺毋滥（精确优先）。
# 反例：「登陆/登录」依赖上下文（诺曼底登陆 合法），不放黑名单，交给 confusion + 模型判断。
# 一行一对，# 开头为注释。

# 形近 / 习惯性错词（整词匹配，安全）
帐号 账号
帐户 账户
做为 作为
按装 安装
报歉 抱歉
必竟 毕竟
渡假 度假
反聩 反馈

# 成语错字（错字唯一、改了不会错）
一如继往 一如既往
谈笑风声 谈笑风生
不径而走 不胫而走
迫不急待 迫不及待
变本加历 变本加厉
穿流不息 川流不息
